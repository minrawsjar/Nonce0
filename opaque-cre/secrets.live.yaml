# Secret ids the workflow asks for → the environment variables that hold them.
# Names only: the values live in .env.live (gitignored, from tools/make-keys.ts)
# and, once `cre secrets create` has run, in the Vault DON.
secretsNames:
  INTENT_KEY_SEED:
    - OPAQUE_SEED_HEX
  CREDENTIAL_MAC:
    - OPAQUE_MAC_HEX
