{
  "name": "@opaque/cre-workflow",
  "version": "1.0.0-review",
  "private": true,
  "type": "module",
  "description": "Opaque V1 — the Chainlink CRE confidential workflow: decrypts a sealed intent inside a Nitro enclave and issues an authenticated release. Swarnim, S4.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "keygen": "node tools/keygen.ts",
    "compile": "bun x cre-compile src/main.ts dist/workflow.wasm"
  },
  "comment": [
    "This package is compiled to WASM and run under Javy (QuickJS), NOT Node.",
    "node:crypto, node:fs, fetch, setTimeout and Buffer do not exist there.",
    "Everything it imports must therefore be pure JS — which is why it pulls",
    "backend/cre/sealed-intent.ts and not backend/mesh, and why release.ts was",
    "ported off node:crypto onto @noble/hashes."
  ],
  "dependencies": {
    "@chainlink/cre-sdk": "^1.20.0",
    "@opaque/protocol-types": "file:../packages/protocol-types",
    "@noble/ciphers": "^2.0.1",
    "@noble/hashes": "^2.4.0",
    "@noble/post-quantum": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^7.0.2"
  }
}
