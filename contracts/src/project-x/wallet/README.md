# Manan - wallet contracts

Implement `PQKeyRegistry` and `PQValidator` here. They must conform to `../interfaces/IPQKeyRegistry.sol` and retain the canonical digest fields: domain, chain ID, wallet address, scheme ID, use counter, payload hash. No ECDSA fallback may rotate the PQ key.
